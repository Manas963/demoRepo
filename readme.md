# Java Program: SumTwoNumbers

This project contains a simple Java program that adds two integers.

## File

- `src/SumTwoNumbers.java` — defines:
  - `sum(int a, int b)`: returns the sum of two numbers.
  - `main(String[] args)`: runs a demo with `10` and `20` and prints the result.

## How to compile

```bash
javac src/SumTwoNumbers.java
```

## How to run

```bash
java -cp src SumTwoNumbers
```

Expected output:

```text
Sum of 10 and 20 is: 30
```
